You are working in the existing FormWorks Editor Viewer / AC Rule Workbench solution.

Use the master FormWorks Editor Viewer spec as the governing direction, but implement Phase 6 only.

Goal:
Hydrate page/document AC rule lists enough to render ordered rule names and basic rule details.

Scope:
Backend parser and frontend rule-list rendering.

Backend:
- Add GET /api/v1/rule-lists/{key}.
- Add GET /api/v1/rules/{key}.
- Hydrate rule list for selected page/document AC process.
- Parse minimum fields:
  - rule key
  - rule name
  - ordinal/order
  - scope: page/document
  - owner page/document key
  - parent rule list key
  - function name if available
  - disabled state if available
  - source refs
  - raw summary
  - parse confidence
- Preserve unknown/partial entries instead of discarding them.
- Generate diagnostics for:
  - rule list failed to load
  - rule entry failed to parse
  - missing rule name
  - missing function name
  - unsupported raw format
  - partial parse

Frontend:
- Selecting page/document AC node hydrates root rule list.
- Render ordered Rule List.
- Selecting a rule updates RuleInspector summary tab.
- Show function name if available.
- Show source/raw availability.
- Show parse diagnostics inline and in inspector.
- Keep Source/Raw tab lazy or placeholder if raw loading is later.

Do not:
- Implement full status-result/action branching yet.
- Implement UDF parsing.
- Implement full parameter mapping.
- Implement full function registry.
- Redesign unrelated UI.
- Add fake rule data.

Deliverable:
Return a complete unified patch with:
1. Rule-list hydration endpoint.
2. Minimal AC rule parser.
3. RuleListView.
4. RuleInspector summary tab.
5. Diagnostics for partial parsing.
6. Backend tests for rule order.
7. Frontend tests for selecting AC node and rule.
8. Build/test commands.
9. Expected behavior.
10. Quick validation steps.

Validation:
- Page-level AC rule lists render in order.
- Document-level AC rule lists render in order.
- Selecting a rule updates inspector.
- Partial parse is visible and honest.