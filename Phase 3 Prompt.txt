You are working in the existing FormWorks Editor Viewer / AC Rule Workbench solution.

Use the master FormWorks Editor Viewer spec as the governing direction, but implement Phase 3 only.

Goal:
Extract the configured FWD hierarchy before parsing AC rules.

Scope:
Implement read-only extraction for:
- Configured batch types.
- Configured document types.
- Configured page types.
- Page variants / graphic variants.
- Fields where available.

Required extraction:
- fwd.BatchNames
- fwd.GetDocsInBatch(batchName)
- fwd.DocumentNames
- fwd.GetPagesInDoc(docName)
- fwd.PageNames
- fwd.VariantNames(pageName)
- fwd.Page(pageName)
- fwd.PageVariant(pageName, variantName)
- page/document/batch STC source refs where safely available through SafeStcReader

Required relationships:
- BatchType -> DocumentType via DocsInBatch
- DocumentType -> PageType via PagesInDoc
- PageType -> PageVariant via VariantNames
- PageType -> Field via page config / field container
- Reverse:
  - DocumentType.parentBatchKeys
  - PageType.parentDocumentKeys

Required endpoints:
- GET /api/v1/fwd/summary
- GET /api/v1/fwd/tree
- GET /api/v1/fwd/batches
- GET /api/v1/fwd/batches/{batchKey}
- GET /api/v1/fwd/documents
- GET /api/v1/fwd/documents/{documentKey}
- GET /api/v1/fwd/pages
- GET /api/v1/fwd/pages/{pageKey}
- GET /api/v1/fwd/pages/{pageKey}/variants

Diagnostics:
- Missing batch .fwdinfo.
- Missing DocsInBatch.
- Missing document .fwdinfo.
- Missing PagesInDoc.
- Missing page variant list.
- Failed field extraction.
- Duplicate display names requiring path disambiguation.
- Hydration failures.

Do not:
- Parse AC rule lists yet.
- Implement page/document AC rule details yet.
- Implement UDF parsing.
- Redesign full UI.
- Add fake data.
- Treat runtime/Inventory batches as configured FWD batch types.

Deliverable:
Return a complete unified patch with:
1. FWD inventory extractor.
2. Relationship builder.
3. API endpoints.
4. Diagnostics.
5. Tests for batch/doc/page/variant relationships.
6. Build/test commands.
7. Expected behavior.
8. Quick validation steps.

Validation:
- Configured batches appear.
- Batch -> documents resolves from DocsInBatch.
- Documents appear.
- Document -> pages resolves from PagesInDoc.
- Pages appear.
- Page -> variants resolves from VariantNames.
- Fields appear where available.
- All data is read-only.