<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <runtime>
	<!-- Allow loading assemblies that NuGet marked with zone information (downloaded from internet) -->
	<loadFromRemoteSources enabled="true" />
  </runtime>
</configuration>