You are working in the existing FormWorks Editor Viewer / AC Rule Workbench solution.

Use the master FormWorks Editor Viewer spec as the governing direction, but implement Phase 1 only.

Goal:
Make FWD access and the API structurally read-only before any UI or parser refactor.

Scope:
- Ensure FWD is opened through the read-only path only.
- Ensure no write-enabled FWD open path is reachable from the viewer/API.
- Add or verify a ReadOnlyGuard service.
- Add a SafeStcReader adapter if missing.
- SafeStcReader may expose only read operations:
  - GetChildNames
  - TryLoadAttrList
  - TryGetChildConfig
  - TryReadObjectData
  - TryReadRuleData
  - TryReadValue
- SafeStcReader must not expose:
  - SetRule
  - SetChildValue
  - SetChildConfig
  - SaveAttrList
  - SaveCMINFO
  - DeleteChild
  - Create
  - Truncate
  - ObjectSave
  - any mutation method
- Remove, block, or guard mutation API endpoints.
- Add defensive errors for mutation attempts:
  “Unavailable in read-only Editor Viewer.”
- Add tests proving mutation paths are absent or blocked.
- Add logging for blocked mutation attempts if a guard is triggered.

Do not:
- Redesign the UI.
- Implement FWD tree redesign.
- Implement AC rule parsing changes.
- Implement UDF parsing changes.
- Change visual CSS beyond removing active mutation affordances if necessary.
- Add fake data.

Deliverable:
Return a complete unified patch with:
1. Read-only FWD open verification.
2. ReadOnlyGuard implementation or update.
3. SafeStcReader implementation or update.
4. Removed/blocked mutation routes.
5. Tests for read-only boundary.
6. Build/test commands.
7. Expected behavior.
8. Quick validation steps.

Validation:
- FWD opens read-only.
- No mutation API endpoint can modify FWD state.
- Unsafe STC write methods are not exposed through read services.
- Tests pass or failures are clearly explained.